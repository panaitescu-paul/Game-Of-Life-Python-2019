import mvc.controller

if __name__ == "__main__":  # only gets inside, if the file (game_of_life.py) is run directly
    mvc.controller.GameOfLifeController()   # make an instance of the controller class
